// ----------------------------------------------------------------------

export default function DatePicker(theme) {
  return {
    MuiDatePicker: {
      defaultProps: {
        inputFormat: 'dd/MM/yyyy',
      },
    },
  }
}
